# Prometheus configuration for ML Evaluation Platform

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "eval-platform-api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics

  - job_name: "eval-platform-consumer"
    static_configs:
      - targets: ["consumer:8001"]
    metrics_path: /metrics

  - job_name: "kafka"
    static_configs:
      - targets: ["kafka:9092"]

  - job_name: "redis"
    static_configs:
      - targets: ["redis:6379"]

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  - "alert_rules.yml"
